# For local development, the app uses the /api proxy configured in vite.config.ts
# This proxy forwards requests to http://localhost:8082 to avoid CORS issues
# Leave VITE_API_URL unset for local development

# For production, set VITE_API_URL to the actual API endpoint:
# VITE_API_URL=https://lending.wayrus.co.ke
